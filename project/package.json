{
  "name": "server-status-monitor",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.9",
    "dns": "^0.2.2",
    "dotenv": "^16.0.3"
  }
}